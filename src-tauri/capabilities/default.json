{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Oris main window: typed repository commands and a directory picker only.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
