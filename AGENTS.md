# Oris 项目规则

总是用中文回答。

## Windows GUI 验证安全约束

以下约束适用于本项目的所有 Windows GUI 自动化、验证脚本及其调用方。禁止干扰用户桌面焦点或误显示其他应用的隐藏窗口。

事故背景：旧测试逻辑按进程名和窗口标题选取首个匹配窗口，再调用 `ShowWindow` / `SetForegroundWindow`，误将 Codex 隐藏的置顶 `avatarOverlay` 显示出来，导致桌面任务栏和标题栏的鼠标点击被拦截，测试结束后也未恢复。不得通过再次操作该非测试窗口来处理验证或清理。

1. **验证前检查危险调用。** 开始 GUI 验证前，必须检查并修正 `scripts/focus-task02-window.ps1` 及其所有调用方，重点检查 `scripts/task02-feedback-*.mjs`。禁止直接复用旧的抢焦点逻辑。
2. **不得操作其他应用窗口。** 禁止通过进程名或窗口标题取第一个匹配窗口后激活；禁止显示、隐藏、移动或修改 Codex、ChatGPT 及其他非测试应用的窗口。
3. **严格限定测试实例。** 自动化仅操作本轮测试创建并准确识别的 Oris 实例。需要原生窗口操作时，必须同时核验 PID、可执行文件完整路径和目标主窗口句柄，排除隐藏辅助窗口；不能准确核验时不得执行该窗口操作。
4. **不得循环抢焦点。** 禁止在等待、轮询、重试循环中反复调用 `SetForegroundWindow`、`AppActivate`、`ShowWindow` 或其他抢焦点操作。用户切走后，不得自动抢回。
5. **优先采用不影响前台的验证方式。** 优先使用不改变系统前台窗口的验证方法。模拟 DOM/CDP 焦点不能作为真实 Windows 前后台切换通过的证据，报告中必须区分这两类验证。
6. **真实焦点测试必须隔离。** 必须进行真实焦点切换测试时，使用专用测试窗口或隔离桌面；若当前环境无法在不干扰用户的条件下完成，明确记录该项未验证并继续其他工作，不得借用 Codex 或其他非测试应用窗口制造失焦。
7. **完整清理测试资源。** 测试结束、失败或中断时，清理本轮创建的测试资源。不得结束用户已有应用，不得遗留覆盖窗口；清理操作同样受上述实例识别和窗口操作边界约束。

### 交付报告要求

涉及 GUI 验证的交付必须说明：修改了哪些危险调用、如何确保不再操作其他应用窗口，以及真实焦点测试的已验证范围和未验证项。不得把模拟焦点、静态检查或未执行的测试表述为真实 Windows 焦点测试通过。
