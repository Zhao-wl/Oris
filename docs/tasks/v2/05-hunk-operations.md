# V2-05 — hunk 级暂存与丢弃

状态：Done（Windows，2026-09-26，[结果](../../validation/v2-05-results.md)）：B15、B16、B17 通过；每个 B15 场景都有前后 git diff / --cached 与工作区、index、refs、stash、config 指纹。未验证：macOS、真实焦点。块操作的 Git 确认时延 P95 约 750 ms，只作参照记录（stage 单文件预算 P95 ≤ 500 ms 不适用于块操作），待用户决定是否设预算。依赖：V2-02；默认在 V1 05 之后（空白规则会影响 hunk 语义）。

## 用户闭环

作为用户，同一个文件里既有要提交的修复又有临时调试代码时，我可以在 diff 中只暂存修复所在的 hunk，并丢弃调试代码所在的 hunk。

## 范围

- 在 diff 阅读器中逐 hunk 暂存（未暂存范围）、取消暂存（已暂存范围）、丢弃（未暂存范围），并排与统一视图都可用。
- **patch 由 Rust 按原始字节生成**，不使用前端解码后文本上算出的 hunk 边界。执行前先 `git apply --check`；与显示时的 revision 不一致时拒绝执行并刷新。
- 前端显示的 hunk 与 Rust 生成的 patch 之间需要建立一一对应关系；对应不上的 hunk 不显示操作按钮，并说明原因。
- 禁用条件：“全部”范围、忽略空白模式、冲突、二进制、子模块、仅 mode 变化、超出内容预算。
- hunk 丢弃纳入 R-DISCARD 的备份与撤销机制。
- 行级操作不在范围内。

## 验收

- [x] B15、B16、B17。
- [x] 每个 B15 场景都用 `git diff` / `git diff --cached` 的前后输出证明只有目标 hunk 发生了移动或被丢弃。
- [x] 写操作后的刷新不丢失当前阅读位置，性能预算没有退步（文件切换类预算复测见结果）。

## 交付与排除

hunk 级暂存与丢弃。不包含行级操作、编辑工作区内容、冲突 hunk 的解决。
