# 05 — 完整阅读体验与特殊文件

状态：Pending（整体未开始；用户授权的“选区/搜索/色标 viewport”阅读切片已在任务 01 原型中提前实施并待总控覆盖）。依赖：02、03 验收通过。默认 04 后执行；与 04 并行需先确认共享 diff 契约和文件隔离，当前不启动。

编号迁移：2026-09-23 原 04 顺延为 05；仅图片实现主责迁入新 03，其他既有约定保留。

二期调整（2026-09-23，[V2-D20](../decisions/v2-decisions.md)）：字号与浅 / 深色的设置入口、持久化和配色方案改由二期任务 [V2-06](v2/06-settings-appearance.md) 交付；本任务继续负责 A06 中字号 / 主题切换后 diff 阅读的正确性，并复用 V2-06 的设置，不另建入口。

## 用户闭环

作为用户，我能用统一易读的文本/图片阅读器理解变化，通过键盘连续浏览，并在大文件/特殊格式不能完整显示时得到准确说明。

## 范围

- 补齐 R-DIFF：搜索、词/行高亮、空白处理、同步滚动、导航计数、上下文逐段/全部展开、软换行、复制、字号/主题/专注模式。
- 蓝修改/绿新增/灰删除、非颜色辅助、连接带，Win/Mac 字体/缩放下仍正确。所有计数/导航/高亮采用同一阅读模型。
- 解码/EOL/无末尾换行、长行/大文件预算与明确降级；编码不支持时不猜成无变化。
- R-IMAGE 图片能力由任务 03 实现并验收；本任务复用，核对主题/键盘/跨入口阅读一致性，不重复建设。
- 二进制、SVG、LFS 指针、submodule gitlink 的明确展示；不执行主动内容或自动下载/初始化。
- R-UX 键盘/可见焦点/平台快捷键；与 02 的保存状态衔接，不持久化大段代码。

## 验收

- [ ] A06 完整：视觉、实际文本选择、搜索、折叠后导航、软换行后对应、字号/主题切换全部可用。
- [ ] A11 特殊文件：其他二进制、SVG、LFS 指针与 submodule gitlink 准确展示、不误报无变化；图片基础由 03 主验，本任务复核集成体验。
- [ ] A12 内容部分：Unicode/中文、CRLF/LF、无末尾换行、编码失败都有直接断言和 UI 状态。
- [ ] 超预算内容明确停止/降级，用户可切换文件；忽略空白不会改变仓库状态或隐藏当前过滤规则。
- [ ] Win11 高 DPI 与 Mac Retina 上实机检查，测试浅深主题及全键盘主要路径。
- [ ] 真实大文本/图片的计算和渲染记录，不使用静态效果图证明性能。

提前实施边界：只覆盖当前文本 Diff 的选区层级、选中同词、应用内搜索和色标槽 viewport 指示；未实施全仓库搜索、替换、专注模式、空白规则、图片/特殊格式、大文件完整体验或双平台最终验收，不能据此勾选 A06 完整或将任务 05（原 04）标为进行中/完成。

## 交付与排除

正式 V1 diff 阅读体验与特殊格式边界。排除连续多文件、blame、语义 diff、Hex/Office/PDF、像素分析、编辑/合并。
